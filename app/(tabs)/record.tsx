import { View } from 'react-native';

// This tab screen is not rendered directly.
// Tapping the tab button navigates to /note/new via tabBarButton in _layout.tsx.
export default function RecordTab() {
  return <View style={{ flex: 1, backgroundColor: '#F2F2F7' }} />;
}
