# CLAUDE.md

本文档为 AI 助手在 noteapp 代码库中工作提供指导，涵盖开发命令、代码规范及行为约束。项目详情请参见 [MVP_DESIGN.md](./MVP_DESIGN.md)。

## 构建和开发命令

```bash
# 安装依赖
npm install

# 运行开发服务器（Expo）- 本地模式
npx expo start

# 运行开发服务器（LAN 模式 - 同一局域网可访问）
npx expo start --lan

# 运行开发服务器（Tunnel 模式 - 用于外网访问）
# 注意：需要安装 @expo/ngrok 包 (npm install --save-dev @expo/ngrok)
# 如果提示 "remote gone away"，可能是网络问题，请改用 --lan 模式
npx expo start --tunnel

# 运行 iOS 模拟器
npx expo run:ios

# 运行 Android 模拟器
npx expo run:android

# 运行测试
npm test

# 代码检查
npm run lint

# 构建生产版本（EAS Build）
eas build --platform all
```

## 设计准则
- 颜色不超过三色，主要有三类颜色：主色、辅助色、强调色
- 弱化颜色的存在，不使用高饱和度的颜色
- 弱化边框，使用留白和间距来区分不同模块，层级来自空间，而非边框
- 元素最小少原则，只保留必要的元素
- 内容优先原则，所有的设计让用户更方便地获取内容
- 一个卡片只解决一个事情
- 呼吸感的动效，动画要自然，让用户感知不到

## 设计风格
项目采用Apple推荐的liquid glass风格

## 代码规范

### 基本原则
- 编写清晰、自文档化的代码
- 保持函数小巧并专注于单一职责
- 使用有意义的变量和函数名称
- 仅为不易理解的复杂逻辑添加注释

### 文件组织
- 按功能或领域对相关文件进行分组
- 将配置文件保留在项目根目录
- 将源代码放在 `src/` 或 `app/` 目录中（Expo Router 使用 `app/`）
- 将测试文件放在源文件旁边或 `__tests__` 目录中

### 提交信息
- 使用祈使语气（"添加功能" 而非 "添加了功能"）
- 第一行保持在 72 个字符以内
- 需要时在正文中包含上下文说明

## 测试策略

推荐方法：
- 核心逻辑的单元测试
- 音频/语音功能的集成测试
- 关键用户流程的端到端测试

## AI 助手指南

在处理此代码库时：

1. **理解上下文**：在进行更改之前阅读相关文件
2. **最小化更改**：仅进行完成任务所必需的更改
3. **避免过度工程**：不添加未请求的功能或抽象
4. **遵循 MVP 范围**：不实现 MVP 范围外的功能（参见 MVP_DESIGN.md）
5. **测试更改**：如有测试可用，在提交前运行测试
6. **清晰的提交**：编写描述性的提交信息，解释"为什么"

### 常见任务

- **添加新功能**：创建必要的文件，更新导入，添加测试
- **修复错误**：识别根本原因，进行最小修复，通过测试验证
- **重构**：确保更改前后测试都能通过
